<Window x:Class="DesktopTools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Tools — 内存清理"
        Width="420"
        Height="360"
        MinWidth="360"
        MinHeight="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="系统内存"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <Border Grid.Row="1"
                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="TotalMemoryText" Text="总内存：—" Margin="0,0,0,4"/>
                <TextBlock x:Name="AvailableMemoryText" Text="可用内存：—" Margin="0,0,0,4"/>
                <TextBlock x:Name="MemoryLoadText" Text="内存负载：—"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <Button x:Name="CleanButton"
                    Content="一键清理"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="CleanButton_Click"/>
            <Button x:Name="RefreshButton"
                    Content="刷新"
                    Width="72"
                    Height="32"
                    Click="RefreshButton_Click"/>
        </StackPanel>

        <Border Grid.Row="3"
                BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="StatusText"
                           Text="尚未执行清理。"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="4"
                   x:Name="CleaningIndicator"
                   Text=""
                   Margin="0,8,0,0"
                   Foreground="Gray"/>
    </Grid>
</Window>
